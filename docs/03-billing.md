# Token、余额与计费模式

[上一章：倍率](02-multiplier.md) · [返回首页](../README.md) · [下一章：快速开始](04-quick-start.md)

## Token 是什么

Token 是模型读取和生成内容时使用的计量单位。它不是严格的“一个汉字”或“一个英文单词”。标点、空格、代码和不同语言都会影响切分方式。

FastAiToken 文档给出的粗略理解是：

- 一个汉字大约占 1～2 Token。
- 一个英文单词大约占 1～2 Token。
- 1000 Token 大约相当于 750 个英文单词或 500 个汉字。

这些只能帮助估算，实际 Token 数以系统记录为准。

## 输入和输出都可能收费

一次对话至少包含两部分：

- **输入 Token**：你发给模型的提示词、历史对话、代码和文档。
- **输出 Token**：模型生成的回答。

很多模型的输入价与输出价不同。即使你只问一句话，客户端可能还会把之前的长对话一起发送，因此对话越长，用量可能越高。

## 余额不是 Token

余额可以理解为钱包里的钱，Token 是本次服务消耗的计量单位。

```text
输入/输出用量 → 按模型价格算出基准费用 → 乘分组倍率 → 从余额扣除
```

因此“还有 10 元余额”不能直接换算成固定数量的提问次数，除非同时知道模型、输入输出长度、倍率和计费规则。

## 按量计费和按次计费

FastAiToken 文档说明平台支持两种常见方式：

| 方式 | 怎么扣费 | 常见场景 |
| --- | --- | --- |
| 按量计费 | 根据输入和输出 Token 计算 | 对话、代码生成、文本与多模态理解 |
| 按次计费 | 每次调用按固定或分档价格计算 | 图片、视频和部分特殊能力 |

不要默认所有模型都按 Token 计费。选择图片、视频或特殊功能前，先看控制台中的单位和说明。

## 一个简化的计算例子

假设某模型本次请求：

- 输入基准费用 0.20 元
- 输出基准费用 0.80 元
- 使用 0.06 倍率分组

那么估算费用为：

```text
(0.20 + 0.80) × 0.06 = 0.06 元
```

这只是讲公式的示例，不代表任何模型的真实价格。真实单价和账单请查看 [平台价格页](https://www.fastaitoken.com/account/pricing) 与使用记录。

## 哪些操作容易消耗更多

- 把一本书或大型代码仓库一次性放进上下文。
- 长对话持续很多轮，却从不新建对话。
- 要求生成特别长的回答。
- 客户端自动重试失败的请求。
- 同时运行多个 Codex 任务。
- 把最大输出长度设置得过高。

## 小白最稳妥的预算方法

1. 先确定一个模型和分组，不频繁切换。
2. 用三到五次典型任务做小额测试。
3. 在平台使用记录里看每次的输入、输出和扣费。
4. 用“平均每次费用 × 每周次数”估算自己的预算。
5. 为 API Key 设置额度或有效期（如果平台支持）。

价格、优惠和倍率会变化，任何静态教程都不能替代实时控制台。下一步进入[注册到第一次请求](04-quick-start.md)。

资料：[FastAiToken 价格说明](https://docs.fastaitoken.com/docs/pricing)
